# Release Runbook

1. Merge a `develop` con CI verde.
2. Merge controlado a `master`.
3. Workflow `release.yml` genera artefactos Windows, Linux y macOS.
4. Validar que el icono personalizado se incluye en el binario.
